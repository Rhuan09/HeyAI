---
name: Tool proposal
about: Propose a new capability
labels: enhancement
---

<!-- Read docs/NON-GOALS.md first. Several categories are closed by design, and the
     reasoning there will answer most proposals faster than a thread will. -->

## What can a user do afterwards that they cannot do today

## Which Windows API backs it

<!-- WinRT, Win32 or COM, and whether it needs package identity or a DispatcherQueue. -->

## How EvaluateRisk classifies the dangerous case from the arguments alone

<!-- Not "this tool is Convenience". A path under Documents and an encoded PowerShell
     command cannot share a tier, so say how the arguments separate them. -->

## What an attacker gains if they inject this request through OCR text

<!-- Assume the model is asked to do this by text on a web page rather than by the user. -->

## Does its output contain anything a third party chooses

<!-- Titles, filenames, device names, screen content. If yes it must be marked untrusted. -->
